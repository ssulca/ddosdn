package org.gstesis.ddos.app;

/**
 * Enum Exec Error type from Clean Code
 */
public enum AppError {
    SUCCESS,
    INVALID,
    NO_SUCH,
    LOCKED,
    NULL,
    EMPTY,
    OUT_OF_RESOURCES,
    WAITING_FOR_EVENT,
    CONCURRENT_ERROR;
}
